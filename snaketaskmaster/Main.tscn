; Placeholder main scene — the entrypoint of the game.
; Wired up via `run/main_scene` in project.godot so F5 / `godot --path .` loads this.
; Replace this with the real gameplay scene (snake board, HUD, etc.) when ready.

[gd_scene format=3]

[node name="Main" type="Node2D"]

[node name="HelloLabel" type="Label" parent="."]
offset_left = 40.0
offset_top = 40.0
offset_right = 300.0
offset_bottom = 70.0
text = "Hello World"
