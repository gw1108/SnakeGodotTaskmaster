
# Taskmaster Loop Progress
# Started: 2026-05-17T02:00:50.864Z
# Preset: default
# Max Iterations: 24

---

## Iter 1 — Task 13: Generate player head sprite (PixelLab)
- Generated 32x32 top-down player head via mcp__pixellab__create_object (object_id 8c367ccb-2f6b-4169-abbf-3c1551435a38, directions=1, view=high top-down).
- Downloaded PNG to snaketaskmaster/assets/sprites/player_head.png (32x32 RGBA, 1070 bytes).
- Learning: PixelLab create_object returns a job ID; must poll get_object until status=completed (~60-180s), then curl the storage URL to disk. The Godot project lives under snaketaskmaster/.

## Iter 2 — Task 18: Project directory structure
- Created snaketaskmaster/assets/audio/, snaketaskmaster/scenes/, snaketaskmaster/scripts/ (assets/sprites/ already existed). Added .gitkeep placeholders so git tracks the empty dirs.
- Verified with `godot --headless --import` — editor loaded cleanly with no errors.
- Learning: task-master CLI isn't pre-installed; `npm i -g task-master-ai` is required at start. MCP `mcp__task-master-ai__*` tools are deferred and need ToolSearch to load schemas before first use.

## Iter 3 — Task 14: Generate player body segment sprite (PixelLab)
- Created 32x32 top-down tileable body segment via mcp__pixellab__create_object (object_id 5652fb6b-a978-4780-89bf-5c9c9d7d6b0b). Downloaded to snaketaskmaster/assets/sprites/player_body.png (32x32 RGBA, 1432 bytes).
- Ran `godot --headless --import` from snaketaskmaster/ — auto-generated player_body.png.import without errors.
- Learning: Bash standalone `sleep N` is blocked in this harness. Use `run_in_background: true` with a powershell Start-Sleep, or Monitor with an until-loop, when waiting for async jobs (e.g. PixelLab generation polls). `cd` into snaketaskmaster/ before `godot --headless --import` since project.godot lives there.

## Iter 4 — Task 15: Generate food sprite (PixelLab) — BLOCKED
- mcp__pixellab__create_object returned "❌ Insufficient generations available". PixelLab account is out of credits/subscription quota.
- Reset task 15 status back to pending. No file changes made.
- Learning: PixelLab quota can be exhausted mid-loop; surface as <loop-blocked> rather than fabricating placeholder art, since downstream tasks expect style-consistent sprites from the same model.

---
# Loop Complete: 2026-05-17T02:11:06.731Z
- Total iterations: 4
- Tasks completed: 3
- Final status: blocked

# Taskmaster Loop Progress
# Started: 2026-05-17T03:50:58.774Z
# Preset: default
# Max Iterations: 21

---

## Iter 1 — Task 15: Generate food sprite (PixelLab) — UNBLOCKED
- Quota refreshed since last loop. Generated 32x32 top-down red apple via mcp__pixellab__create_object (object_id 3975ec86-5dbf-48d0-8867-7cad72d15f19). Downloaded to snaketaskmaster/assets/sprites/food.png (32x32 RGBA, 808 bytes).
- Ran `godot --headless --import` from snaketaskmaster/ — auto-generated food.png.import without errors.
- Learning: PixelLab generation time can vary wildly. Today's run took ~20 minutes despite ETA repeatedly reporting ~150s; the ETA field is unreliable when their backend is congested. Continue polling at ~3-5min intervals rather than trusting ETA. Also: when curl-ing with backslash Windows paths inside Bash, paths get mangled — use forward-slash paths or quote heavily.

## Iter 2 — Task 19: GameConstants autoload + grid coord utilities
- Wrote snaketaskmaster/scripts/game_constants.gd with GRID_WIDTH=20, GRID_HEIGHT=15, CELL_SIZE=32, TICK_INTERVAL=0.15 and helpers grid_to_pixel/pixel_to_grid/is_valid_grid_pos.
- Registered as autoload "GameConstants" via [autoload] block in snaketaskmaster/project.godot.
- Added scripts/test_game_constants.gd; `godot --headless --script res://scripts/test_game_constants.gd` → "OK: all GameConstants tests passed" (covers constants, both conversions, and 5 is_valid_grid_pos boundary cases).
- Learning: When running a `extends SceneTree` test via `--script`, project autoloads are NOT instantiated, so referencing the autoload by its registered name fails compile. Load the script with `load("res://...").new()` inside the test instead. (`--headless --import` is what actually validates the autoload registration.)

# Taskmaster Loop Progress
# Started: 2026-05-17T04:25:03.921Z
# Preset: default
# Max Iterations: 19

---

## Iter 1 — Task 21: InputManager autoload (arrow keys + WASD with reversal blocking)
- Wrote snaketaskmaster/scripts/input_manager.gd: _input handles ui_up/ui_down/ui_left/ui_right (arrow keys via default actions) plus KEY_W/A/S/D fallback, storing latest dir in buffered_direction. get_buffered_direction(current) clears buffer on every call and returns current when buffer is empty or would reverse 180°.
- Registered as autoload "InputManager" in snaketaskmaster/project.godot.
- Added scripts/test_input_manager.gd; `godot --headless --script res://scripts/test_input_manager.gd` → "OK: all InputManager tests passed". Covers WASD, arrow keys, empty buffer, 180° block (both axes), perpendicular turns, buffer-clear-on-blocked-retrieval, and latest-input-wins.
- Learning: in headless `--script` test, autoloads aren't instantiated and `Input.parse_input_event` requires a window/main loop. Calling `IM._input(synthetic_event)` directly is the simplest way to exercise input handling. `is_action_pressed("ui_up")` on a synthetic InputEventKey with `keycode=KEY_UP` works in headless because ui_* actions are bound by the engine defaults loaded from project.godot.

## Iter 2 — Task 23: FoodManager (spawn + collision)
- Wrote snaketaskmaster/scripts/food_manager.gd extending Node2D: signal food_eaten; `current_food_position: Vector2i`; `food_sprite: Sprite2D` created in `_ready()` loading res://assets/sprites/food.png. `spawn_food(occupied_cells)` builds available-cell list across full grid minus occupied, picks uniformly via `randi() % size`, positions sprite via GameConstants.grid_to_pixel. Returns false (and leaves state unchanged) when grid is full. `check_collision(head)` emits food_eaten and returns true on match.
- Test scene scenes/test_food_manager.tscn + scripts/test_food_manager.gd (extends Node). `godot --headless res://scenes/test_food_manager.tscn` → "OK: all FoodManager tests passed". Covers spawn validity/avoidance of occupied cells, sprite positioning + visibility, collision signal emission, non-collision no-signal, near-full grid (forced cell), full grid returns false with preserved state.
- Learning: scripts that reference autoloads (e.g. `GameConstants.GRID_WIDTH`) cannot be tested via `extends SceneTree` + `--script` because autoloads aren't bound in that mode and compile fails. Workaround: scene-based test — create a `.tscn` with a Node + test script (extends Node), run via `godot --headless <scene_path>`. Godot loads autoloads when running a scene normally. The positional scene path overrides project.godot's main_scene.

## Iter 3 — Task 20: Player entity (head/body chain, growth, occupancy)
- Wrote snaketaskmaster/scripts/player.gd extends Node2D. State: `segments: Array[Vector2i]` initialized to [(10,7),(9,7),(8,7)] with segments[0] as head; `current_direction`/`next_direction` default RIGHT; `grow_pending: int`. `_ready()` loads player_head.png + player_body.png, creates `_head_sprite` + N-1 `_body_sprites`. `move(dir)`: computes new_head = head+dir; if grow_pending>0 inserts new_head at front + appends a body sprite + decrements grow_pending; else shifts segments forward 1 in-place. Then `_sync_sprites()` updates positions via GameConstants.grid_to_pixel and rotates head by `Vector2(current_direction).angle()`. `add_growth()`, `get_head_position()`, `occupies_position()` straightforward.
- Test scene scenes/test_player.tscn + scripts/test_player.gd. `godot --headless res://scenes/test_player.tscn` → "OK: all Player tests passed". Covers initial state, head+body sprite creation, plain move, perpendicular turn, single growth (length +1, tail preserved, body sprite count +1), occupies_position true/false, two queued growths consumed across two moves, head sprite position sync after compound moves.
- Learning: For in-place segment shift, iterate descending (`range(size-1, 0, -1)`) so each slot reads the not-yet-overwritten predecessor. Head rotation comes free from `Vector2(current_direction).angle()` since Vector2.RIGHT.angle()==0 — assumes the head sprite was generated facing right (PixelLab top-down head defaulted to that orientation here).

## Iter 4 — Task 22: Tick-based game loop and movement system
- Wrote snaketaskmaster/scripts/game_loop.gd extends Node. Owns a child Timer (wait_time=GameConstants.TICK_INTERVAL=0.15, one_shot=false, autostart=false). Signals: tick, wall_collision, self_collision. `@export var player`. `start_game()` sets is_active=true and starts the timer; `stop_game()` reverses. `_on_tick()` pulls next direction via `InputManager.get_buffered_direction(player.current_direction)`, assigns `player.current_direction`, calls `player.move(next)`, then checks `!GameConstants.is_valid_grid_pos(head)` for wall collision and segments[1..] contains head for self-collision; on collision emits signal + stop_game, otherwise emits tick.
- Test scene scenes/test_game_loop.tscn + scripts/test_game_loop.gd (extends Node, awaits a real `get_tree().create_timer` for the cadence test). `godot --headless res://scenes/test_game_loop.tscn` → "OK: all GameLoop tests passed". Covers timer config, start/stop toggling, basic tick advance, buffered direction picked up, 180° reversal blocked via InputManager, wall collision emit+stop, self-collision emit+stop, real-timer cadence (≥2 ticks in 2.5×interval), inactive-loop no-op.
- Learning: Reassigning a typed `Array[Vector2i]` field (e.g. `player.segments = [Vector2i(...), ...]`) with an untyped array literal raises a runtime "Invalid assignment of property" and silently aborts the rest of the awaiting function — but `_ready` still resumes after `await` and prints OK on empty failures, so the test looked green while skipping cases. Fix: build the typed value separately (`segments.clear(); segments.append(...)`) or assign through a `var x: Array[Vector2i] = [...]`. Watch for this pattern whenever a test mutates typed-array state on another node.

## Iter 5 — Task 25: Wall collision detection + GameState autoload
- Wall-collision detection was already wired in game_loop.gd from task 22, but the spec required a `game_over()` method and a `GameState` autoload tracking `collision_type` ("wall" / "self"). Added scripts/game_state.gd (single field + reset()), registered as autoload "GameState" in project.godot. Refactored game_loop._on_tick → calls new `game_over(type: String)` which sets `GameState.collision_type`, stops the timer, and emits the matching signal. Both wall and self paths now route through it (self is task 26 but the existing branch was trivial to align).
- Extended scripts/test_game_loop.gd test 6: asserts `GameState.collision_type == "wall"` after right-wall hit. Added test 6b looping over all four walls (left x=-1, right x=GRID_WIDTH, top y=-1, bottom y=GRID_HEIGHT) — builds body trailing opposite of dir to avoid false self-collision, resets InputManager.buffered_direction between iterations. `godot --headless res://scenes/test_game_loop.tscn` → "OK: all GameLoop tests passed". Re-ran test_player.tscn + test_food_manager.tscn — both still green (new autoload didn't break anything).
- Learning: GDScript variables are function-scoped, not block-scoped. Declaring `var dir` inside a `for case in wall_cases:` loop collides with an earlier `var dir` in a different test block of the same function. Parser error "There is already a variable named 'dir' declared in this scope" — rename one of them (used `wall_dir` / `wall_start`). Worth knowing because Python/JS habits suggest the loop body has its own scope; it doesn't.

## Iter 6 — Task 26: Self-collision detection
- Core check was already wired in game_loop.gd:45-48 from iter 5 (loops segments[1..n] vs head, calls game_over("self")). Task 26's spec required explicit verification of GameState.collision_type and behavior across body depths, so this iter is test-only hardening.
- Updated test_game_loop.gd test 7 to reset GameState first and assert `GameState.collision_type == "self"` post-collision. Added test 7b (head curls back and hits segments[5], not the adjacent segments[1]; uses add_growth() to freeze the tail so the target cell doesn't vacate that tick) and test 7c (1-segment head-only snake never fires self_collision). `godot --headless res://scenes/test_game_loop.tscn` → OK. Re-ran test_player.tscn and test_food_manager.tscn — both still green.
- Learning: when constructing a snake layout where the head will collide with a specific body segment N ticks later, the tail must be pinned via `add_growth()` *before* the collision tick. Otherwise the in-place shift in player.move() rotates segments[k] = segments[k-1] for k>0 and the target cell evacuates before the collision check sees it. The U-turn test (7) only worked because segments[1] is fed by segments[0] (head) which moves *into* the same cell. For deeper-segment collisions, growth pin is mandatory.

## Iter 7 — Task 24: Wire food eating to growth + score
- Extended GameState autoload with `current_score`, `high_score`, `add_score`, `reset_score`, `update_high_score`. `reset()` zeros current_score but preserves high_score across runs.
- Added `@export var food_manager` + `signal food_eaten` to game_loop.gd. After wall/self collision checks, calls `food_manager.check_collision(head)` and on hit does `GameState.add_score(1)` → `player.add_growth()` → `food_manager.spawn_food(player.segments)` → emit local food_eaten.
- New scripts/test_food_integration.gd + scenes/test_food_integration.tscn (followed scene-based pattern from food/game_loop tests). Covers GameState helpers, single eat (score+1, grow_pending+1, sprite respawn off old cell + off body), 3 consecutive eats with drain-tick verification, high_score sticky across reset(), and null food_manager no-op. `godot --headless res://scenes/test_food_integration.tscn` → OK; all four pre-existing test suites still green.
- Learning: snake growth materializes on the move *after* `add_growth()`, not the eat tick itself. After N consecutive eat-ticks length is `initial + N - 1` with `grow_pending == 1` still queued — needed one extra non-eating tick to drain it for the final length assertion. Originally asserted `initial + N` and the test failed before this got internalized.

## Iter 8 — Task 16: Generate background floor tile asset (PixelLab)
- Generated via mcp__pixellab__create_tiles_pro (tile_id 61f0b45b-6b9b-495c-9843-1397c120e3d6) with description "1). grass floor tile, seamless, simple pattern", tile_size=32, tile_type=square_topdown, tile_view=top-down. Returned 16 variations; downloaded tile_0.png → snaketaskmaster/assets/sprites/floor_tile.png (32x32 RGBA, 1432 bytes). `godot --headless --import` succeeded.
- Learning: create_tiles_pro returns N tile variations (here 16) at the same B2 URL pattern; pick one and save with the desired filename. tile_view=top-down gives no depth — important for a seamless floor that tiles across a TileMap without isometric/depth artifacts. ETA was ~169s and was accurate this time (unlike create_object's flaky ETA).
