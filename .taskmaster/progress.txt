
# Taskmaster Loop Progress
# Started: 2026-05-17T02:00:50.864Z
# Preset: default
# Max Iterations: 24

---

## Iter 1 — Task 13: Generate player head sprite (PixelLab)
- Generated 32x32 top-down player head via mcp__pixellab__create_object (object_id 8c367ccb-2f6b-4169-abbf-3c1551435a38, directions=1, view=high top-down).
- Downloaded PNG to snaketaskmaster/assets/sprites/player_head.png (32x32 RGBA, 1070 bytes).
- Learning: PixelLab create_object returns a job ID; must poll get_object until status=completed (~60-180s), then curl the storage URL to disk. The Godot project lives under snaketaskmaster/.

## Iter 2 — Task 18: Project directory structure
- Created snaketaskmaster/assets/audio/, snaketaskmaster/scenes/, snaketaskmaster/scripts/ (assets/sprites/ already existed). Added .gitkeep placeholders so git tracks the empty dirs.
- Verified with `godot --headless --import` — editor loaded cleanly with no errors.
- Learning: task-master CLI isn't pre-installed; `npm i -g task-master-ai` is required at start. MCP `mcp__task-master-ai__*` tools are deferred and need ToolSearch to load schemas before first use.

## Iter 3 — Task 14: Generate player body segment sprite (PixelLab)
- Created 32x32 top-down tileable body segment via mcp__pixellab__create_object (object_id 5652fb6b-a978-4780-89bf-5c9c9d7d6b0b). Downloaded to snaketaskmaster/assets/sprites/player_body.png (32x32 RGBA, 1432 bytes).
- Ran `godot --headless --import` from snaketaskmaster/ — auto-generated player_body.png.import without errors.
- Learning: Bash standalone `sleep N` is blocked in this harness. Use `run_in_background: true` with a powershell Start-Sleep, or Monitor with an until-loop, when waiting for async jobs (e.g. PixelLab generation polls). `cd` into snaketaskmaster/ before `godot --headless --import` since project.godot lives there.

## Iter 4 — Task 15: Generate food sprite (PixelLab) — BLOCKED
- mcp__pixellab__create_object returned "❌ Insufficient generations available". PixelLab account is out of credits/subscription quota.
- Reset task 15 status back to pending. No file changes made.
- Learning: PixelLab quota can be exhausted mid-loop; surface as <loop-blocked> rather than fabricating placeholder art, since downstream tasks expect style-consistent sprites from the same model.

---
# Loop Complete: 2026-05-17T02:11:06.731Z
- Total iterations: 4
- Tasks completed: 3
- Final status: blocked

# Taskmaster Loop Progress
# Started: 2026-05-17T03:50:58.774Z
# Preset: default
# Max Iterations: 21

---

## Iter 1 — Task 15: Generate food sprite (PixelLab) — UNBLOCKED
- Quota refreshed since last loop. Generated 32x32 top-down red apple via mcp__pixellab__create_object (object_id 3975ec86-5dbf-48d0-8867-7cad72d15f19). Downloaded to snaketaskmaster/assets/sprites/food.png (32x32 RGBA, 808 bytes).
- Ran `godot --headless --import` from snaketaskmaster/ — auto-generated food.png.import without errors.
- Learning: PixelLab generation time can vary wildly. Today's run took ~20 minutes despite ETA repeatedly reporting ~150s; the ETA field is unreliable when their backend is congested. Continue polling at ~3-5min intervals rather than trusting ETA. Also: when curl-ing with backslash Windows paths inside Bash, paths get mangled — use forward-slash paths or quote heavily.

## Iter 2 — Task 19: GameConstants autoload + grid coord utilities
- Wrote snaketaskmaster/scripts/game_constants.gd with GRID_WIDTH=20, GRID_HEIGHT=15, CELL_SIZE=32, TICK_INTERVAL=0.15 and helpers grid_to_pixel/pixel_to_grid/is_valid_grid_pos.
- Registered as autoload "GameConstants" via [autoload] block in snaketaskmaster/project.godot.
- Added scripts/test_game_constants.gd; `godot --headless --script res://scripts/test_game_constants.gd` → "OK: all GameConstants tests passed" (covers constants, both conversions, and 5 is_valid_grid_pos boundary cases).
- Learning: When running a `extends SceneTree` test via `--script`, project autoloads are NOT instantiated, so referencing the autoload by its registered name fails compile. Load the script with `load("res://...").new()` inside the test instead. (`--headless --import` is what actually validates the autoload registration.)

# Taskmaster Loop Progress
# Started: 2026-05-17T04:25:03.921Z
# Preset: default
# Max Iterations: 19

---

## Iter 1 — Task 21: InputManager autoload (arrow keys + WASD with reversal blocking)
- Wrote snaketaskmaster/scripts/input_manager.gd: _input handles ui_up/ui_down/ui_left/ui_right (arrow keys via default actions) plus KEY_W/A/S/D fallback, storing latest dir in buffered_direction. get_buffered_direction(current) clears buffer on every call and returns current when buffer is empty or would reverse 180°.
- Registered as autoload "InputManager" in snaketaskmaster/project.godot.
- Added scripts/test_input_manager.gd; `godot --headless --script res://scripts/test_input_manager.gd` → "OK: all InputManager tests passed". Covers WASD, arrow keys, empty buffer, 180° block (both axes), perpendicular turns, buffer-clear-on-blocked-retrieval, and latest-input-wins.
- Learning: in headless `--script` test, autoloads aren't instantiated and `Input.parse_input_event` requires a window/main loop. Calling `IM._input(synthetic_event)` directly is the simplest way to exercise input handling. `is_action_pressed("ui_up")` on a synthetic InputEventKey with `keycode=KEY_UP` works in headless because ui_* actions are bound by the engine defaults loaded from project.godot.

## Iter 2 — Task 23: FoodManager (spawn + collision)
- Wrote snaketaskmaster/scripts/food_manager.gd extending Node2D: signal food_eaten; `current_food_position: Vector2i`; `food_sprite: Sprite2D` created in `_ready()` loading res://assets/sprites/food.png. `spawn_food(occupied_cells)` builds available-cell list across full grid minus occupied, picks uniformly via `randi() % size`, positions sprite via GameConstants.grid_to_pixel. Returns false (and leaves state unchanged) when grid is full. `check_collision(head)` emits food_eaten and returns true on match.
- Test scene scenes/test_food_manager.tscn + scripts/test_food_manager.gd (extends Node). `godot --headless res://scenes/test_food_manager.tscn` → "OK: all FoodManager tests passed". Covers spawn validity/avoidance of occupied cells, sprite positioning + visibility, collision signal emission, non-collision no-signal, near-full grid (forced cell), full grid returns false with preserved state.
- Learning: scripts that reference autoloads (e.g. `GameConstants.GRID_WIDTH`) cannot be tested via `extends SceneTree` + `--script` because autoloads aren't bound in that mode and compile fails. Workaround: scene-based test — create a `.tscn` with a Node + test script (extends Node), run via `godot --headless <scene_path>`. Godot loads autoloads when running a scene normally. The positional scene path overrides project.godot's main_scene.

## Iter 3 — Task 20: Player entity (head/body chain, growth, occupancy)
- Wrote snaketaskmaster/scripts/player.gd extends Node2D. State: `segments: Array[Vector2i]` initialized to [(10,7),(9,7),(8,7)] with segments[0] as head; `current_direction`/`next_direction` default RIGHT; `grow_pending: int`. `_ready()` loads player_head.png + player_body.png, creates `_head_sprite` + N-1 `_body_sprites`. `move(dir)`: computes new_head = head+dir; if grow_pending>0 inserts new_head at front + appends a body sprite + decrements grow_pending; else shifts segments forward 1 in-place. Then `_sync_sprites()` updates positions via GameConstants.grid_to_pixel and rotates head by `Vector2(current_direction).angle()`. `add_growth()`, `get_head_position()`, `occupies_position()` straightforward.
- Test scene scenes/test_player.tscn + scripts/test_player.gd. `godot --headless res://scenes/test_player.tscn` → "OK: all Player tests passed". Covers initial state, head+body sprite creation, plain move, perpendicular turn, single growth (length +1, tail preserved, body sprite count +1), occupies_position true/false, two queued growths consumed across two moves, head sprite position sync after compound moves.
- Learning: For in-place segment shift, iterate descending (`range(size-1, 0, -1)`) so each slot reads the not-yet-overwritten predecessor. Head rotation comes free from `Vector2(current_direction).angle()` since Vector2.RIGHT.angle()==0 — assumes the head sprite was generated facing right (PixelLab top-down head defaulted to that orientation here).

## Iter 4 — Task 22: Tick-based game loop and movement system
- Wrote snaketaskmaster/scripts/game_loop.gd extends Node. Owns a child Timer (wait_time=GameConstants.TICK_INTERVAL=0.15, one_shot=false, autostart=false). Signals: tick, wall_collision, self_collision. `@export var player`. `start_game()` sets is_active=true and starts the timer; `stop_game()` reverses. `_on_tick()` pulls next direction via `InputManager.get_buffered_direction(player.current_direction)`, assigns `player.current_direction`, calls `player.move(next)`, then checks `!GameConstants.is_valid_grid_pos(head)` for wall collision and segments[1..] contains head for self-collision; on collision emits signal + stop_game, otherwise emits tick.
- Test scene scenes/test_game_loop.tscn + scripts/test_game_loop.gd (extends Node, awaits a real `get_tree().create_timer` for the cadence test). `godot --headless res://scenes/test_game_loop.tscn` → "OK: all GameLoop tests passed". Covers timer config, start/stop toggling, basic tick advance, buffered direction picked up, 180° reversal blocked via InputManager, wall collision emit+stop, self-collision emit+stop, real-timer cadence (≥2 ticks in 2.5×interval), inactive-loop no-op.
- Learning: Reassigning a typed `Array[Vector2i]` field (e.g. `player.segments = [Vector2i(...), ...]`) with an untyped array literal raises a runtime "Invalid assignment of property" and silently aborts the rest of the awaiting function — but `_ready` still resumes after `await` and prints OK on empty failures, so the test looked green while skipping cases. Fix: build the typed value separately (`segments.clear(); segments.append(...)`) or assign through a `var x: Array[Vector2i] = [...]`. Watch for this pattern whenever a test mutates typed-array state on another node.

## Iter 5 — Task 25: Wall collision detection + GameState autoload
- Wall-collision detection was already wired in game_loop.gd from task 22, but the spec required a `game_over()` method and a `GameState` autoload tracking `collision_type` ("wall" / "self"). Added scripts/game_state.gd (single field + reset()), registered as autoload "GameState" in project.godot. Refactored game_loop._on_tick → calls new `game_over(type: String)` which sets `GameState.collision_type`, stops the timer, and emits the matching signal. Both wall and self paths now route through it (self is task 26 but the existing branch was trivial to align).
- Extended scripts/test_game_loop.gd test 6: asserts `GameState.collision_type == "wall"` after right-wall hit. Added test 6b looping over all four walls (left x=-1, right x=GRID_WIDTH, top y=-1, bottom y=GRID_HEIGHT) — builds body trailing opposite of dir to avoid false self-collision, resets InputManager.buffered_direction between iterations. `godot --headless res://scenes/test_game_loop.tscn` → "OK: all GameLoop tests passed". Re-ran test_player.tscn + test_food_manager.tscn — both still green (new autoload didn't break anything).
- Learning: GDScript variables are function-scoped, not block-scoped. Declaring `var dir` inside a `for case in wall_cases:` loop collides with an earlier `var dir` in a different test block of the same function. Parser error "There is already a variable named 'dir' declared in this scope" — rename one of them (used `wall_dir` / `wall_start`). Worth knowing because Python/JS habits suggest the loop body has its own scope; it doesn't.

## Iter 6 — Task 26: Self-collision detection
- Core check was already wired in game_loop.gd:45-48 from iter 5 (loops segments[1..n] vs head, calls game_over("self")). Task 26's spec required explicit verification of GameState.collision_type and behavior across body depths, so this iter is test-only hardening.
- Updated test_game_loop.gd test 7 to reset GameState first and assert `GameState.collision_type == "self"` post-collision. Added test 7b (head curls back and hits segments[5], not the adjacent segments[1]; uses add_growth() to freeze the tail so the target cell doesn't vacate that tick) and test 7c (1-segment head-only snake never fires self_collision). `godot --headless res://scenes/test_game_loop.tscn` → OK. Re-ran test_player.tscn and test_food_manager.tscn — both still green.
- Learning: when constructing a snake layout where the head will collide with a specific body segment N ticks later, the tail must be pinned via `add_growth()` *before* the collision tick. Otherwise the in-place shift in player.move() rotates segments[k] = segments[k-1] for k>0 and the target cell evacuates before the collision check sees it. The U-turn test (7) only worked because segments[1] is fed by segments[0] (head) which moves *into* the same cell. For deeper-segment collisions, growth pin is mandatory.

## Iter 7 — Task 24: Wire food eating to growth + score
- Extended GameState autoload with `current_score`, `high_score`, `add_score`, `reset_score`, `update_high_score`. `reset()` zeros current_score but preserves high_score across runs.
- Added `@export var food_manager` + `signal food_eaten` to game_loop.gd. After wall/self collision checks, calls `food_manager.check_collision(head)` and on hit does `GameState.add_score(1)` → `player.add_growth()` → `food_manager.spawn_food(player.segments)` → emit local food_eaten.
- New scripts/test_food_integration.gd + scenes/test_food_integration.tscn (followed scene-based pattern from food/game_loop tests). Covers GameState helpers, single eat (score+1, grow_pending+1, sprite respawn off old cell + off body), 3 consecutive eats with drain-tick verification, high_score sticky across reset(), and null food_manager no-op. `godot --headless res://scenes/test_food_integration.tscn` → OK; all four pre-existing test suites still green.
- Learning: snake growth materializes on the move *after* `add_growth()`, not the eat tick itself. After N consecutive eat-ticks length is `initial + N - 1` with `grow_pending == 1` still queued — needed one extra non-eating tick to drain it for the final length assertion. Originally asserted `initial + N` and the test failed before this got internalized.

## Iter 8 — Task 16: Generate background floor tile asset (PixelLab)
- Generated via mcp__pixellab__create_tiles_pro (tile_id 61f0b45b-6b9b-495c-9843-1397c120e3d6) with description "1). grass floor tile, seamless, simple pattern", tile_size=32, tile_type=square_topdown, tile_view=top-down. Returned 16 variations; downloaded tile_0.png → snaketaskmaster/assets/sprites/floor_tile.png (32x32 RGBA, 1432 bytes). `godot --headless --import` succeeded.
- Learning: create_tiles_pro returns N tile variations (here 16) at the same B2 URL pattern; pick one and save with the desired filename. tile_view=top-down gives no depth — important for a seamless floor that tiles across a TileMap without isometric/depth artifacts. ETA was ~169s and was accurate this time (unlike create_object's flaky ETA).

## Iter 9 — Task 17: Generate wall border tile asset (PixelLab)
- Generated via mcp__pixellab__create_tiles_pro (tile_id 4049c639-402e-4ecc-9d82-a8de03150c69) with description "1). wall tile, border, solid barrier, stone or brick", tile_size=32, tile_type=square_topdown, tile_view=low top-down. Returned 16 variations; downloaded tile_0.png → snaketaskmaster/assets/sprites/wall_tile.png (32x32 RGBA, 651 bytes). `godot --headless --import` succeeded.
- Learning: ETA ~180s held accurate again for create_tiles_pro (two iters in a row), consistent with iter 8's observation that tile generation ETAs are reliable while create_object ETAs are not. Verified pixel dimensions via `[System.Drawing.Image]::FromFile(...).Width/Height` in PowerShell — useful one-liner when ImageMagick isn't available on Windows.

## Iter 10 — Task 34: Window settings and integer pixel scaling
- Edited snaketaskmaster/project.godot: added [display] block with window/size/viewport_width=640, viewport_height=480, size/mode=0 (windowed), stretch/mode="viewport", stretch/aspect="keep"; added rendering/2d/snap/snap_2d_transforms_to_pixel=true and snap_2d_vertices_to_pixel=true under [rendering].
- New scripts/test_window_settings.gd + scenes/test_window_settings.tscn — asserts all 7 ProjectSettings values plus a cross-check that viewport dims equal GameConstants.GRID_WIDTH/HEIGHT * CELL_SIZE. `godot --headless res://scenes/test_window_settings.tscn` → "OK: all window settings tests passed". `godot --headless --import` clean. Re-ran all 5 prior test suites (game_constants, input_manager, food_manager, player, game_loop, food_integration) — all green.
- Learning: ProjectSettings keys do NOT include the leading section name as a separate prefix component — write the full slash path with section included, e.g. `ProjectSettings.get_setting("display/window/size/viewport_width")` and `"rendering/2d/snap/snap_2d_transforms_to_pixel"`. The section header in project.godot just groups them on disk; the key seen at runtime is `section/the/rest`.

## Iter 11 — Task 28: Title screen scene
- Wrote scripts/title_screen.gd extends CanvasLayer with const GAME_SCENE_PATH="res://scenes/game.tscn", `started` flag, `_ready` calls refresh_high_score + _start_blink, `_input` accepts first non-echo pressed InputEventKey and calls start_game(). `refresh_high_score` writes `"High Score: %d" % GameState.high_score`. `_start_blink` runs a looping Tween toggling prompt_label.modulate:a between 0.25 and 1.0. `start_game` guards via `ResourceLoader.exists(GAME_SCENE_PATH)` so the still-missing game.tscn doesn't crash; falls back to push_warning.
- Wrote scenes/title_screen.tscn: CanvasLayer root → Control (full-rect, mouse_filter=2) → VBoxContainer (full-rect, alignment=1 centered) holding TitleLabel ("2D Grid Survival Game", 32px), Spacer1 (24px), HighScoreLabel ("High Score: 0", 20px), Spacer2 (48px), PromptLabel ("Press any key to start", 16px). All labels horizontal_alignment=1 (HORIZONTAL_ALIGNMENT_CENTER).
- scripts/test_title_screen.gd + scenes/test_title_screen.tscn cover: scene instantiation + node presence, label text + center alignment, high-score binding from GameState on instantiation, refresh_high_score live update, key press sets `started` flag (intentionally triggers the ResourceLoader.exists guard since game.tscn not yet built — push_warning expected in output), second key ignored, key-release/echo/mouse events ignored, high score persists across re-instantiation. `godot --headless res://scenes/test_title_screen.tscn` → "OK: all title screen tests passed". Re-ran food_integration, window_settings, game_loop — all green.
- Learning: Tween blink (`create_tween().set_loops().tween_property(label, "modulate:a", ...)`) is much lighter than an AnimationPlayer + Animation resource for a simple 2-step blink — no .res file needed, lives entirely in the script. Also: `ResourceLoader.exists(path)` is the cleanest pre-`change_scene_to_file` guard when a referenced scene is built in a later task — avoids hard-crash without needing test-only shims, and emits a push_warning visible in logs.

## Iter 12 — Task 27: Playfield rendering (floor + wall border)
- Wrote scripts/playfield.gd extends Node2D. `_ready` builds two fresh TileSet resources (one for floor_tile.png, one for wall_tile.png) — each with tile_size=Vector2i(32,32) and a single TileSetAtlasSource holding atlas tile (0,0) — and assigns them to `$FloorLayer` / `$WallLayer` (TileMapLayer nodes). Then populates: FloorLayer fills x∈[0..19] × y∈[0..14] (300 cells), WallLayer fills the border (x=-1, x=20 for y∈[-1..15]; y=-1, y=15 for x∈[-1..20]) for 2*(20+2) + 2*15 = 74 cells. Layers stay at position (0,0); GameConstants.CELL_SIZE drives the tile size.
- Wrote scenes/playfield.tscn with a Node2D root + two empty TileMapLayer children (script populates tile_set + cells at runtime, no .tres files needed).
- scripts/test_playfield.gd + scenes/test_playfield.tscn instance Playfield and after one process_frame verify: layer offsets are zero, floor has exactly 300 cells all at source 0 / atlas (0,0), wall has exactly 74 border cells (incl. all four corners), no floor/wall overlap, each layer's tile_set is a fresh 32×32 TileSet with one atlas source containing tile (0,0), and TileMapLayer.map_to_local((0,0))==Vector2(16,16) (cell center; top-left of (0,0) is pixel (0,0) as spec requires). `godot --headless res://scenes/test_playfield.tscn` → "OK: all Playfield tests passed". Re-ran test_food_integration, test_game_loop, test_title_screen, test_window_settings — all green.
- Learning: Godot 4 `TileMapLayer.map_to_local` returns the tile *center*, not its top-left. For a 32×32 cell at grid (0,0), center is (16,16), not (0,0) — useful to remember when wiring sprites that already sit at the pixel computed by `GameConstants.grid_to_pixel` (which returns the top-left). The two coordinate conventions live side-by-side, so any pixel comparison between a TileMapLayer-derived position and a Sprite2D.position needs the +/- (CELL_SIZE/2) offset. Also: TileSet/TileSetAtlasSource can be built entirely in code (no .tres needed) — `TileSet.new()` → set `tile_size` → `add_source(atlas_source, id)` after the atlas calls `create_tile(atlas_coord)`. Much simpler than authoring TileSet .tres files by hand.

## Iter 13 — Task 30: Main game scene integrating all gameplay components
- Wrote scripts/game.gd extends Node2D and scenes/game.tscn assembling Playfield (instance of playfield.tscn) + Player + FoodManager + GameLoop + HUD/CanvasLayer with a top-left ScoreLabel. `_ready` runs GameState.reset(), wires game_loop.player/food_manager (export refs assigned in code, not authored as NodePaths in the .tscn), connects food_eaten→_refresh_score plus wall_collision/self_collision→_on_game_over, calls food_manager.spawn_food(player.segments), refreshes the score label, then game_loop.start_game(). _on_game_over guards on `ResourceLoader.exists("res://scenes/game_over_screen.tscn")` so the missing scene from later task doesn't crash — push_warning instead.
- scripts/test_game.gd + scenes/test_game.tscn: covers scene-load + child presence (Playfield/Player/FoodManager/GameLoop/HUD/ScoreLabel), GameState reset on entry, game_loop wired + active, food spawned at valid non-overlapping cell with sprite visible, HUD "Score: 0" initial text, eat-tick increments score + refreshes HUD + queues growth, wall-collision sets GameState.collision_type="wall" + stops loop (push_warning expected from missing game_over screen). `godot --headless res://scenes/test_game.tscn` → "OK: all Game integration tests passed". Re-ran test_game_loop, test_food_integration, test_playfield, test_food_manager, test_player, test_window_settings, test_input_manager, test_title_screen → all green.
- Learning: completing a scene-load target that was previously absent can break sibling tests that depended on the guard branch firing. test_title_screen had been passing only because `ResourceLoader.exists("res://scenes/game.tscn")` returned false → push_warning → no scene change. With game.tscn now existing, the simulated key press triggered a REAL `change_scene_to_file` which freed the test's root node mid-test and hung the await chain. Fix: converted title_screen.gd's GAME_SCENE_PATH const to a settable `var game_scene_path`, and updated test_title_screen to override it with a nonexistent path before triggering input. Rule of thumb for this project: any test that exercises a "guard for missing future scene" code path should explicitly inject the bad path rather than rely on the absence of the production asset — the production asset eventually exists.

## Iter 14 — Task 31: HUD score display polish (panel + contrast)
- Score wiring + label already existed from task 30 (game.gd._on_food_eaten → _refresh_score). Task 31 spec calls for "readable font size and contrasting color" plus "semi-transparent background panel for readability". Updated scenes/game.tscn: added a StyleBoxFlat sub_resource (bg_color=Color(0,0,0,0.5), 4px corner radius), inserted ScorePanel (Panel, offsets 4/2/132/30) BEFORE ScoreLabel in HUD so it draws behind, and added `theme_override_colors/font_color = Color(1,1,1,1)` on ScoreLabel for white-on-dark contrast.
- Extended scripts/test_game.gd test 5 to assert: font_color override exists + fully opaque, font_size ≥14, label sits in top-left half of viewport, ScorePanel exists with a StyleBoxFlat panel override whose bg alpha is in (0,1). Added test 7b looping 3 consecutive eats with HUD text re-check each tick to verify real-time updates. `godot --headless res://scenes/test_game.tscn` → OK. Re-ran the other 8 suites (game_constants, input_manager, food_manager, player, game_loop, food_integration, window_settings, title_screen, playfield) — all green.
- Learning: in a .tscn, sibling draw order is declaration order — the Panel must be authored before the Label so the label renders on top. Also, `theme_override_styles/panel = SubResource("...")` is how you attach an inline StyleBoxFlat without a separate .tres; this is preferable to authoring `theme.tres` for one-off HUD decoration. Verifying a stylebox in headless: `panel.has_theme_stylebox_override("panel")` followed by `get_theme_stylebox("panel") as StyleBoxFlat` — the cast can fail (return null) if you pick the wrong type, so always null-check rather than assuming.

## Iter 15 — Task 29: Game-over screen (final score + new-high indicator + restart)
- Added `previous_high_score: int` field to GameState; `reset()` now snapshots `high_score` into it at the start of each run. Needed because `add_score()` bumps `high_score` mid-run, so by game-over time `current_score == high_score` even after merely tying — the snapshot lets the game-over screen check `current_score > previous_high_score` for a *strict* new high.
- Wrote scripts/game_over_screen.gd (CanvasLayer): on `_ready` binds final-score label, toggles NewHighScoreLabel visibility on `current_score > previous_high_score`, and conditionally renders a CollisionLabel ("You hit a wall" / "You ran into yourself") based on `GameState.collision_type`. Tween-blinks the prompt label, and on first non-echo key press calls `restart()` → `change_scene_to_file(title_scene_path)`. Path is a settable `var` (default "res://scenes/title_screen.tscn") so tests can inject a nonexistent path and exercise the guard branch.
- Wrote scenes/game_over_screen.tscn with VBoxContainer holding TitleLabel ("Game Over", 32px) → ScoreLabel (20px) → CollisionLabel (14px) → NewHighScoreLabel (20px, yellow Color(1,0.9,0.2,1)) → PromptLabel ("Press any key to restart", 16px). All horizontal_alignment=center.
- Refactored game.gd: turned `const GAME_OVER_SCENE_PATH` into settable `var game_over_scene_path` (same overridable-path pattern title_screen.gd uses) so test_game.gd can inject a nonexistent path before triggering the wall-collision test. Without this, completing task 29 would have made `ResourceLoader.exists(...)` return true in test_game.gd, causing the wall-collision test to call `change_scene_to_file` and free the test root mid-test — same regression-pattern noted in iter 13's title_screen lesson.
- scripts/test_game_over_screen.gd + scenes/test_game_over_screen.tscn cover: scene-load + 5 child labels, default `Final Score: 0` rendering, score-binding from GameState, new-high banner visibility (strictly-greater rule including tied-score-is-not-new test), collision-label hidden on empty collision_type / visible+messaged for "wall" and "self", key press flips `restarted` flag (guard branch logs expected push_warning), second key no-op, release/echo/mouse-event ignored, and high_score survives instantiation untouched. `godot --headless res://scenes/test_game_over_screen.tscn` → OK. Re-ran all 9 other suites — all green.
- Learning: any guarded "future scene" reference in production code is a forward time-bomb for the sibling test — when that task lands, the guard collapses to a real `change_scene_to_file`, freeing the test root mid-await and the test silently hangs (or worse, prints OK via failures-list pattern). Lesson from iter 13 reapplied here proactively: every scene-change path in production code is now an overridable `var`, and every test instance gets a nonexistent path injected before exercising the input that could trigger it. This is now the project's standing pattern — apply to all future scene-transition code.

## Iter 16 — Task 32: Placeholder eat-food SFX
- Generated a synthetic blip WAV procedurally with PowerShell (120ms, mono 22050Hz/16-bit PCM, 800Hz first half / 1200Hz second half "ding", 10ms fade-in + 40ms fade-out envelope at amplitude 0.4) → snaketaskmaster/assets/audio/eat_food.wav (5336 bytes incl. 44-byte RIFF header). `godot --headless --import` recognized it as AudioStream cleanly.
- Added `[ext_resource type="AudioStream" ...]` + `EatSound` AudioStreamPlayer node (volume_db=-6.0) to scenes/game.tscn. Wired `game.gd._on_food_eaten` to call `eat_sound.play()` after the existing `_refresh_score()` (null-guards on both node and `.stream`).
- Extended scripts/test_game.gd: test 6b asserts EatSound node + stream loaded + volume_db ≤ 0 + not pre-playing; test 7 now also asserts `eat_player.playing` after the forced food-eat tick. `godot --headless res://scenes/test_game.tscn` → OK. Re-ran all 11 suites — all green.
- Learning: a usable placeholder WAV doesn't need any external tooling. PowerShell can write the 44-byte RIFF header + raw 16-bit PCM samples via `[BitConverter]` + `BinaryWriter` in <40 lines, and Godot's headless --import accepts it as AudioStream without ceremony. Also: in headless Godot 4, AudioStreamPlayer.play() still flips `.playing=true` even without an audio device (no driver is initialized but the playback bookkeeping runs), so `eat_player.playing` is a reliable post-condition for "play() was invoked" in tests — no need for an audio-device mock.

## Iter 17 — Task 33: Placeholder death SFX
- Generated 450ms descending "death" WAV procedurally with PowerShell (mono 22050Hz/16-bit PCM, three pitch segments 500Hz → 250Hz → 125Hz at 30/60/100% boundaries, 10ms fade-in + 80ms fade-out envelope at amplitude 0.4) → snaketaskmaster/assets/audio/death.wav (19888 bytes). `godot --headless --import` recognized it as AudioStream cleanly.
- Added `[ext_resource type="AudioStream" ...]` + `DeathSound` AudioStreamPlayer node (volume_db=-4.0) to scenes/game.tscn. Updated `game.gd._on_game_over` to play the sound first, then `await get_tree().create_timer(0.5).timeout` to let it audible-finish, then `is_inside_tree()`-guarded `change_scene_to_file`. Both wall_collision and self_collision signals route through `_on_game_over` so a single handler covers both.
- Extended scripts/test_game.gd: test 6c asserts DeathSound node + stream loaded + volume_db ≤ 0 + not pre-playing; existing test 8 (wall collision) now asserts `death_player.playing`; new test 9 covers self-collision with the growth-pinned-tail trick (head boxes around to segments[3] after add_growth) and asserts `collision_type=="self"` + death sound playing. `godot --headless res://scenes/test_game.tscn` → OK. Re-ran all 10 other suites — all green.
- Learning: scene-transition handlers that play SFX need `await ... timeout` before `change_scene_to_file` so the AudioStreamPlayer isn't freed mid-playback when the current scene is unloaded. Pair it with `is_inside_tree()` post-await — the node may have been queue_freed by a test (or in production by a parent close) during the wait. Also: for self-collision tests, you cannot trigger a 180° flip via InputManager — it explicitly blocks that — so the only way to force a self-hit on a short snake without a multi-tick path is `player.add_growth()` before the colliding tick to pin the tail so segments[k] doesn't vacate when shifted.

## Iter 18 — Task 35: Final integration testing + polish
- Polish fix: `project.godot` had `run/main_scene="res://Main.tscn"` (a nonexistent file from the initial template). Repointed to `res://scenes/title_screen.tscn` so launching the game (e.g. `godot --path snaketaskmaster`) actually boots into the title screen instead of erroring.
- Added scripts/test_integration.gd + scenes/test_integration.tscn — end-to-end test exercising the title → game → game-over → restart cycle as instantiated children (never via change_scene_to_file, so the test root survives). Coverage: (1) main_scene config; (2) title screen high-score binding + key press; (3) first run: 3 forced eats + wall collision routes through game_over with collision_type="wall" and DeathSound.playing; (4) game-over labels (Final Score, "You hit a wall", new-high banner visible because previous_high_score=0); (5) second run: high_score persists (3), current_score resets (0), previous_high_score snapshots 3; tie the prior high exactly (3 eats) → high_score still 3; self-collision routes "self"; (6) game-over after tie: new-high banner NOT visible (3 is not strictly > 3), collision label "You ran into yourself"; (7) input buffering — perpendicular DOWN applied, 180° reversal UP blocked while moving DOWN (snake continues DOWN by 1 cell), perpendicular LEFT applied next tick; (8) real-timer tick cadence — ≥2 ticks fire in 2.5×TICK_INTERVAL. `godot --headless res://scenes/test_integration.tscn` → "OK: all integration tests passed". Re-ran all 11 prior suites — all green.
- Learning: GDScript lambdas capture int locals **by value**, so `var n: int = 0; signal.connect(func(): n += 1)` will leave the outer `n` at 0 even when the signal fires repeatedly. The existing test_game_loop cadence test sidesteps this with `var fired: Array[int] = [0]` and increments `fired[0]` — the Array is a reference type. I burned one iteration of my cadence assertion missing this and had to swap to the Array-slot pattern. Make this the standing pattern for any signal-counting closure in test code.

# Taskmaster Loop Progress
# Started: 2026-05-19T00:40:09.859Z
# Preset: default
# Max Iterations: 15

---

## Iter 1 — Task 1: Godot project scaffolding
- Deleted root-level Main.tscn placeholder. Created scenes/game.tscn with Node2D "Game" root + Playfield (Node2D) + HUD (CanvasLayer) + GameStateMachine (Node) children. Attached scripts/game.gd with @onready refs for the three children (no state machine call yet — that's task 15).
- Updated project.godot: added [input] block with move_up/down/left/right actions (each mapped to arrow key + corresponding WASD key, full canonical InputEventKey form), repointed run/main_scene to res://scenes/game.tscn.
- New test/game_test.gd (gdUnit4): 6 tests cover scene file existence, scene-instance child presence + types (Node2D/CanvasLayer/Node), all 4 input actions defined with ≥2 events each, exact keycode coverage for both arrow and WASD bindings per action, 640×480 viewport config, and main_scene pointer correctness. `godot --headless --path . -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd --ignoreHeadlessMode -a test/` → 7/7 pass (including the pre-existing verify_gdunit4 smoke test).
- Learning: project.godot [input] entries need the FULL InputEventKey canonical form (all 16 fields: resource_local_to_scene, resource_name, device, window_id, all modifier_pressed flags, pressed, keycode, physical_keycode, key_label, unicode, location, echo, script) — a stripped-down form like `Object(InputEventKey,"keycode":87)` won't parse. Godot KEY_* values: KEY_UP=4194320, DOWN=4194322, LEFT=4194319, RIGHT=4194321; WASD are ASCII (W=87, A=65, S=83, D=68). Restart action was specced as "any key" so it's intentionally not in InputMap — that'll be handled in code at game-over state in a later task. The project was reset between loops (scripts/ + scenes/ + assets/ are gone, sprites moved from assets/sprites/ to source/sprites/, audio to source/audio/), so prior loop's work is gone but the new 15-task plan is the source of truth.

## Iter 2 — Task 2: Grid system autoload
- Wrote snaketaskmaster/scripts/grid.gd: CELL_SIZE=32, GRID_WIDTH=18, GRID_HEIGHT=13 (interior; total 20x15 incl. 1-cell wall border on each side). Helpers: `grid_to_world` returns cell center, `world_to_grid` floors to cell, `is_wall` true on x∈{0,19} or y∈{0,14}, `get_interior_cells` returns the 18*13=234 non-wall cells. Registered as autoload "Grid" via new `[autoload]` block in project.godot (above `[display]`).
- New test/grid_test.gd (gdUnit4): 8 tests cover constants, total dimensions fitting 640x480 viewport, grid→world cell-center mapping, world→grid floor behavior at cell boundaries (0/16/31.9/32/48), full inverse round-trip across all 20×15 cells, is_wall coverage of all four border rows/cols, is_wall=false for full interior, and get_interior_cells size + corner spot-check + uniqueness. `godot --headless ... -a test/grid_test.gd` → 8/8 pass; full `-a test/` run → 15/15 across 3 suites.
- Learning: `world_to_grid` needs explicit floor — `Vector2i(world_pos / CELL_SIZE)` in GDScript truncates toward zero (so negatives round up), which is fine for now but `(v / float(CELL_SIZE)).floor()` is safer for future negative-position handling and matches the inverse-of-grid_to_world property test. Also: autoload registration in project.godot is just a single `[autoload]` section with `Name="*res://path/to/script.gd"` (the `*` enables it); no .uid juggling needed since Godot regenerates the .uid on next import. Once registered the `Grid` singleton is accessible by name in any script *and* in gdUnit4 tests (gdUnit4 runs inside the Godot project context, so autoloads are live).

## Iter 3 — Task 3: Playfield tile rendering
- Wrote snaketaskmaster/scripts/playfield.gd extends Node2D: `_ready` builds a single TileSet in-code with two TileSetAtlasSource entries (FLOOR_SOURCE_ID=0 → source/sprites/floor_tile.png, WALL_SOURCE_ID=1 → source/sprites/wall_tile.png), tile_size=Vector2i(Grid.CELL_SIZE, Grid.CELL_SIZE), each source has a single atlas tile at (0,0). Then loops the full 20×15 grid and calls `tilemap.set_cell(cell, WALL_SOURCE_ID if Grid.is_wall(cell) else FLOOR_SOURCE_ID, Vector2i(0,0))`.
- Wrote scenes/playfield.tscn (Node2D root + TileMapLayer child — no .tres tileset, built at runtime). Replaced the inline `Playfield` Node2D in scenes/game.tscn with `instance=ExtResource("...playfield.tscn")` so the script wires up automatically. game.gd's `$Playfield` @onready ref still works.
- New test/playfield_test.gd (gdUnit4, 7 tests): scene exists, TileMapLayer child present + correct type, tileset has both atlas sources with 32×32 tile_size and atlas tile (0,0), full grid painted (300 cells), border vs interior source-id correctness with explicit floor=234 / wall=66 counts, every painted cell uses atlas origin (0,0), game.tscn instance also paints the full grid. `godot --headless ... -a test/` → 22/22 across 4 suites.
- Learning: When a child scene instance owns a script, the parent .tscn doesn't need `script=ExtResource(...)` on that child — the `instance=ExtResource("playfield.tscn")` line carries the script. Also, in gdUnit4 you can `auto_free(packed.instantiate()); add_child(...)` and the node's `_ready` runs immediately on `add_child` — no need for `await get_tree().process_frame` to verify post-_ready state (this differs from the bare scene-based tests in the prior loop). Reading `tilemap.get_cell_source_id(cell)` after _ready works without any frame yields.

## Iter 4 — Task 4: Game tick (fixed-interval timer + signal)
- Wrote snaketaskmaster/scripts/game_tick.gd extends Node: const TICK_RATE=0.15, signal tick_occurred, `@onready var timer = $Timer`. `_ready` enforces timer.wait_time=0.15 / one_shot=false / autostart=false and connects timer.timeout→_on_timer_timeout. Public API: `start_tick()`, `stop_tick()`, `is_running()` (= `not timer.is_stopped()`). `_on_timer_timeout` just emits `tick_occurred`.
- Wrote scenes/game_tick.tscn (Node root + Timer child wait_time=0.15). Added `[node name="GameTick" parent="." instance=ExtResource("3_game_tick")]` to scenes/game.tscn and `@onready var game_tick: Node = $GameTick` to scripts/game.gd. Lazy/non-autostart — the not-yet-existing state machine (task 15) will be responsible for calling start_tick/stop_tick.
- New test/game_tick_test.gd (gdUnit4, 9 tests): scene+script file existence, TICK_RATE constant via `script.get_script_constant_map()`, child Timer presence + wait_time/one_shot/autostart config, signal declared via `get_signal_list()`, no emission before start_tick (300ms wait), ≥2 ticks in 400ms after start_tick (cadence), stop_tick halts emission (snapshot+300ms re-check), restart-after-stop resumes, and game.tscn contains a GameTick instance with the configured Timer. `godot --headless ... -a test/` → 31/31 across 5 suites.
- Learning: gdUnit4 test methods can `await get_tree().create_timer(N).timeout` for real-time waits — the Timer in the SUT advances normally during that wait even in headless. Reading a script constant from a test without instantiating: `(load(path) as GDScript).get_script_constant_map()["NAME"]` returns the value directly. And: GDScript lambdas capture int locals by value (iter 18 lesson reapplied here) — every signal-count test uses `var fired: Array[int] = [0]; ... fired[0] += 1` so the closure mutates a reference, not a frozen capture.

## Iter 5 — Task 5: Player input (InputHandler component)
- Wrote snaketaskmaster/scripts/input_handler.gd extends Node: enum Direction {UP, DOWN, LEFT, RIGHT}, `current_direction: int = Direction.RIGHT`, `buffered_direction: Variant = null`. `_input(event)` dispatches on `event.is_action_pressed("move_up/down/left/right")` and calls `_buffer_direction(dir)`. `_buffer_direction` sets buffered only if not opposite of current. `_is_opposite` covers all 4 reversal pairs. `consume_buffered_direction()` returns buffered (and sets current_direction=buffered, clears buffer) if non-null else returns current_direction. No scene needed — script attaches directly to a Node; state machine (task 15) will own the instance.
- New test/input_handler_test.gd (gdUnit4, 15 tests): script file existence, enum keys exact order, initial state, arrow-key buffering, full WASD walk through a legal-perpendicular cycle (W→A→S→D each consumed), all 4 reversal pairs rejected for arrows AND WASD (8 cases), all 8 perpendicular legal cases buffered, consume resets buffer + updates current_direction, consume with no buffer returns current unchanged, latest legal input overwrites prior buffer, rejected input preserves existing buffer, key-release events don't buffer, echo events don't buffer, unrelated keys (SPACE/ENTER) don't buffer. `godot --headless ... -a test/input_handler_test.gd` → 15/15 pass; full `-a test/` → 46/46 across 6 suites.
- Learning: `event.is_action_pressed("name")` works in headless gdUnit4 when `_input(synthetic_event)` is invoked directly — Godot's action lookup runs against InputMap which is populated from project.godot regardless of windowing mode. This avoids the `Input.is_action_just_pressed` approach in the task pseudo-code (which depends on frame timing and would need real event-pump). Also: in GDScript test loops, `for case in cases:` where `cases` is `Array[Array]` works but `case` is untyped — indexing with `case[0]/case[1]` is fine for parameterized tests. gdUnit4 helpfully prints a warning at startup that InputEvents don't propagate in headless; the warning is harmless when you bypass the engine and call `_input()` directly with a synthetic event.

## Iter 6 — Task 6: Snake movement and growth
- Wrote snaketaskmaster/scripts/snake.gd extends Node2D: `body: Array[Vector2i]`, `current_direction: int = InputHandlerScript.Direction.RIGHT`, `grow_next_tick: bool`. `_ready` initializes body to [center, center-(1,0), center-(2,0)] using Grid.GRID_WIDTH/2 = (9,6) — three cells facing right, all interior. `move(dir)`: `body.push_front(body[0] + direction_to_vector(dir))`, then pop_back unless grow_next_tick (in which case clear the flag and keep tail). Always sets current_direction=dir. `schedule_growth()` flips the flag; `get_head()` returns body[0]; `direction_to_vector(dir)` matches on InputHandlerScript.Direction enum to Vector2i.
- New test/snake_test.gd (gdUnit4, 13 tests): script existence, initial state (centered, length 3, RIGHT facing, grow flag false, all cells in interior), direction_to_vector for all 4 enum values, move-right tail-drop, move-each-direction parameterized, current_direction update, growth-keeps-tail with flag reset, schedule_growth flag flip, growth-flag-resets-after-one-move (next move drops tail again), get_head return, perpendicular turn body shift, 3-consecutive-growths (length grows 1 per move from 3→4→5→6). `godot --headless ... -a test/snake_test.gd` → 13/13 pass; full `-a test/` → 59/59 across 7 suites.
- Learning: typed `Array[Vector2i]` assignment from an untyped literal fails silently (iter 4 lesson from prior loop) — also applies inside `_ready` of a freshly-loaded script. Worked around by `var initial: Array[Vector2i] = [...]; body = initial`. In tests, the same restriction applies when overriding `snake.body = [...]` — built a `_set_body(snake, cells)` helper that constructs a typed `Array[Vector2i]` and `append`s before assignment. Centralizing this in a helper avoids the silent-skip pattern that iter 4 originally tripped over.

## Iter 7 — Task 8: Food logic (spawn + collision)
- Wrote snaketaskmaster/scripts/food.gd extends Node: `signal food_eaten`, `position: Vector2i = ZERO`. `spawn(occupied_cells: Array[Vector2i]) -> bool` builds an occupied-cell dict, walks `Grid.get_interior_cells()` filtering out occupied, picks `randi() % available.size()`, returns false (state unchanged) on full grid. `check_collision(head_pos) -> bool` emits `food_eaten` and returns true on match. Scope held to the component + tests — full GameTick→Food wiring deferred to task 15 (state machine) since Snake/Food/Input aren't yet composed into game.tscn and that's natural state-machine territory.
- New test/food_test.gd (gdUnit4, 11 tests): script existence, signal declared via `get_signal_list()`, default position, spawn returns true + interior placement + cell ∈ get_interior_cells(), spawn forced into single-free-cell scenario (pop one from interior, fill the rest, spawn 20× → always lands there), spawn returns false when ALL interior cells occupied (no state mutation expected), 50× spawn against a 4-cell snake body never lands on occupied, collision true on exact match, collision false on three near-miss cells, signal fires only on hit (closure-counted via `Array[int] = [0]` per iter 18 lesson, two consecutive hits → counter=2), and 100-spawn variance check (`seen.size() > 1`). All 11 pass; full `-a test/` → 70/70 across 8 suites.
- Learning: gdUnit4's `assert_array(arr).contains([v])` and `.not_contains([v])` take an Array of expected items, NOT a single value — passing `assert_array(...).contains(my_vec2i)` parses as "contains an array" and fails confusingly. Always wrap in `[v]` even for a single-element membership check. Also: the `Array[Vector2i]` typed-literal restriction (iter 6's `_set_body` workaround) extends to test fixtures here — built `_to_typed(cells)` helper for the same reason so test arrays always have the typed marker before reaching `spawn(occupied_cells: Array[Vector2i])`.

## Iter 8 — Task 11: Collision detection (Snake.check_collision)
- Added `check_collision() -> bool` to snake.gd: returns true if `Grid.is_wall(body[0])` or if any segment in body[1..] equals head. Pure function, no signal — Game.gd tick-handler wiring deferred to task 15 (state machine) per the same scope-control pattern as task 8 (food). Snake remains a passive component; orchestration belongs in the state machine.
- Extended test/snake_test.gd with 9 collision tests: false on clean interior body, true on each of the 4 walls (left x=0, right x=GRID_WIDTH+1=19, top y=0, bottom y=GRID_HEIGHT+1=14), true on head-overlap-segment[3], false for 1-segment snake (no body to collide with), true after `move()` into right-wall, true after `move()` into segments[5] (used `schedule_growth()` before the colliding tick to pin tail — iter 17 lesson reapplied). All 22 snake tests pass; full `-a test/` → 79/79 across 8 suites.
- Learning: Grid wall semantics changed from prior loop. Now GRID_WIDTH=18/GRID_HEIGHT=13 describe INTERIOR cells, and walls live at x∈{0, GRID_WIDTH+1} and y∈{0, GRID_HEIGHT+1} (total grid 20×15). So `Grid.is_wall(Vector2i(GRID_WIDTH, 5))` is FALSE — that's the rightmost interior cell. Tests must use `GRID_WIDTH + 1` for the right wall coordinate. Easy to miss because the prior loop's Grid had walls at GRID_WIDTH itself (interior was 0..GRID_WIDTH-1). Always re-check is_wall semantics rather than assuming the off-by-one convention.

## Iter 9 — Task 15: Game state machine (PLAYING / GAME_OVER)
- Wrote snaketaskmaster/scripts/game_state_machine.gd extends Node: enum State {PLAYING, GAME_OVER}, `current_state` defaults to PLAYING, `signal state_changed(new_state: int)`, `transition_to(new_state)` short-circuits if equal else calls `_exit_state(old) → assign → _enter_state(new) → emit`. `_enter_state` looks up `get_parent().get_node_or_null("GameTick")` (null-safe so standalone tests work) and calls start_tick/stop_tick per state. Attached the script to the existing GameStateMachine node in scenes/game.tscn via `script = ExtResource(...)` line.
- New test/game_state_machine_test.gd (gdUnit4, 10 tests): script file existence, enum State has PLAYING+GAME_OVER (distinct values), initial state PLAYING, state_changed signal declared, transition updates current_state, transition emits signal with new state, same-state transition is a no-op (no signal, no state change), PLAYING→GAME_OVER→PLAYING round-trip emits twice in order, multi-call pattern emits only on distinct transitions (3 distinct out of 5 calls → 2 emissions because first call PLAYING→PLAYING is no-op), and full game.tscn integration verifying GameTick.is_running() flips with state transitions. `godot --headless ... -a test/` → 89/89 across 9 suites.
- Learning: `(load(path) as GDScript).get_script_constant_map()` works, but `const SCRIPT := preload(path); SCRIPT.get_script_constant_map()` does NOT — Godot 4.6 throws "Cannot call non-static function on the class directly. Make an instance instead." The `const` form gets parsed as static class-type access. Workaround: stash the script in a helper `func _gsm_script() -> GDScript: return load(...) as GDScript` and call methods through that. Same gotcha presumably applies to accessing nested enums via `SCRIPT.State.PLAYING` on a const — I sidestepped by snapshotting the enum dict into `_PLAYING`/`_GAME_OVER` vars in `before_test()`. Cleaner anyway since enum values stay readable across many tests.

## Iter 11 — Task 9: Food rendering (Sprite2D positioned via Grid.grid_to_world)
- Extended scripts/food.gd: added FOOD_TEXTURE_PATH const, `sprite: Sprite2D` field, `_ready` creates the sprite + add_child + initial position via Grid.grid_to_world(position). `spawn()` now updates sprite.position after a successful cell pick; failed spawn (no cells available) leaves sprite.position untouched. Kept Food as `extends Node` (Sprite2D inherits transform from the nearest CanvasItem ancestor when the script is added under the game scene Node2D), per the task pseudo-code — avoided changing to Node2D which would have shadowed `position: Vector2i` against Node2D's `position: Vector2`.
- Extended test/food_test.gd with 4 rendering tests: sprite created as child with food.png texture, sprite.position matches Grid.grid_to_world(food.position) after forced-single-cell spawn, sprite.position updates on each of 20 random spawns, and sprite/position unchanged when spawn returns false (filled-grid scenario). `godot --headless ... -a test/food_test.gd` → 15/15 pass; full `-a test/` → 101/101 across 9 suites.
- Learning: `Node2D.position` is `Vector2` — declaring `var position: Vector2i` on a Node2D-extending script collides with the inherited property. For grid-cell-state objects that also need to render, either (a) keep the script `extends Node` and rely on a CanvasItem ancestor for the Sprite2D child, or (b) rename the cell field to `cell` / `grid_position` and extend Node2D. Option (a) is the minimal-diff route when an existing test API already uses `.position` for grid coords (food_test.gd does — `f.position = Vector2i(4, 7)`).

## Iter 10 — Task 7: Snake rendering (head + body sprite pool)
- Extended scripts/snake.gd with Option B (Sprite2D pool). Added constants HEAD_TEXTURE_PATH / BODY_TEXTURE_PATH pointing at res://source/sprites/player_head.png and player_body.png. `_ready` now calls `_init_rendering()` (creates head_sprite Sprite2D + assigns head texture, add_child) then `_update_rendering()`. `_update_rendering()` sets head_sprite.position = Grid.grid_to_world(body[0]) and head_sprite.rotation = Vector2(direction_to_vector(current_direction)).angle(); maintains body_sprites pool size = body.size()-1 by appending new Sprite2Ds (body texture) or queue_free()-ing extras; positions each body sprite at Grid.grid_to_world(body[i+1]). `move()` now calls `_update_rendering()` after segment shift so head + body track without external orchestration.
- Extended test/snake_test.gd by 8 tests: head sprite created + Sprite2D + correct texture (differs from body), head texture identity equal to load(player_head.png), head positioned at grid_to_world(body[0]), body_sprites size = body.size()-1 with body texture, body sprites positioned at body[1..], head sprite position updates after move() + body sprites trail along old cells, head rotation matches direction (RIGHT=0, DOWN=PI/2, LEFT=±PI, UP=-PI/2), and pool grows by 1 after schedule_growth()+move(). `godot --headless ... -a test/` → 97/97 across 9 suites.
- Learning: gdUnit4 / GDScript test files can't infer typed locals from `s.body_sprites.size()` — parser error "Cannot infer the type of 'initial_count' variable because the value doesn't have a set type" when using `:=`. Explicit `var initial_count: int = ...` fixes it. Happens specifically with field access on a dynamically-typed Node2D (test file doesn't type-annotate `s`). Easy to hit when porting `:=` patterns from production code into tests. Also: Sprite2D draws centered on .position by default, so Grid.grid_to_world (which returns cell-center) lines them up directly — no offset math needed.

## Iter 13 — Task 14: Game-over screen + restart wiring
- Extended scripts/hud.gd with `show_game_over(final_score)` / `hide_game_over()` + `final_score_label` @onready ref. Extended scenes/hud.tscn with a centered GameOverPanel (Panel, 320×180, anchored to screen center, StyleBoxFlat semi-transparent black) containing a VBoxContainer with GameOverLabel ("Game Over", 28px) + FinalScoreLabel (20px) + RestartPromptLabel ("Press any key to restart", 16px). Panel `visible=false` by default.
- Added `reset()` method to scripts/snake.gd — calls `_initialize_body()` (restores 3-segment centered body), resets `current_direction=RIGHT`, `grow_next_tick=false`, then `_update_rendering()` so head/body sprites snap back. Body sprite pool auto-shrinks to size 2 via the existing pop_back+queue_free loop in _update_rendering.
- Extended scripts/game.gd: added `score: int` field, `_ready` connects state_machine.state_changed → `_on_state_changed`. `_on_state_changed(new_state)` calls `hud.show_game_over(score)` on GAME_OVER, `hud.hide_game_over()` on PLAYING. `_input(event)` accepts first non-echo pressed InputEventKey while in GAME_OVER and calls `restart()`. `restart()` resets score to 0, calls `snake.reset()` if a Snake child exists, calls `food.spawn(snake.body cells)` if Food child exists, refreshes HUD (update_score 0 + hide_game_over), then `state_machine.transition_to(PLAYING)` which auto-starts game_tick via the existing _enter_state hook. Snake/Food are defensively `get_node_or_null`'d so the current scene (which has neither wired in yet) doesn't crash.
- Extended test/hud_test.gd by 5 tests (panel hidden by default, 3 required labels with exact text, show_game_over visibility+score binding, latest-value-wins, hide_game_over flips visible back). Extended test/snake_test.gd by 2 reset tests (typed-Array body restored to center + defaults; sprite pool shrinks + positions snap). Extended test/game_test.gd by 9 tests (initial hidden state, GAME_OVER transition shows overlay with current score, PLAYING transition hides, key press in PLAYING ignored, key press in GAME_OVER calls restart, key release/echo ignored, restart resets score+state+HUD label+panel, restart re-starts GameTick). `godot --headless ... -a test/` → 128/128 across 10 suites.
- Learning: ProjectSettings InputMap is initialized eagerly at scene-load even in headless gdUnit4 — `event.is_action_pressed("name")` would have worked here too, but using the simpler `event is InputEventKey and event.pressed and not event.echo` "any key" check matches the task spec ("Press any key to restart") exactly and avoids tying restart to game-action keys. Also confirmed: in Godot 4.6, when a CanvasLayer scene is embedded via `instance=ExtResource(hud.tscn)` in a parent .tscn, the parent's `$HUD` resolves to the embedded scene's root and `hud.show_game_over(...)` calls through to the script attached at that root — no script-attachment proxy needed. The pattern from iter 12 (promote inline → instance) extends cleanly to adding sub-components later.

## Iter 12 — Task 13: Score HUD (CanvasLayer + update_score)
- Wrote snaketaskmaster/scripts/hud.gd extends CanvasLayer with `@onready var score_label: Label = $ScoreLabel` and `update_score(new_score: int)` setting `score_label.text = "Score: %d" % new_score` (null-guards score_label so a standalone CanvasLayer.new() can no-op). Wrote scenes/hud.tscn: CanvasLayer root with HUD script + ScoreLabel child (Label, offsets 8/4/200/32, theme_override_font_sizes/font_size=20, default text "Score: 0"). Refactored scenes/game.tscn: replaced inline `[node name="HUD" type="CanvasLayer"]` with `instance=ExtResource("scenes/hud.tscn")` so the script + label wire automatically. game.gd's `$HUD` @onready ref still resolves. No game.gd score-tracking added yet — Game.gd composition with Snake/Food is deferred to a later task (same scope-control pattern as tasks 8/11 deferred orchestration).
- New test/hud_test.gd (gdUnit4, 11 tests): script + scene file existence, scene instantiates as CanvasLayer, ScoreLabel child is Label, default text "Score: 0", update_score(0/5/9999) formats correctly, latest-value-wins after sequential calls, game.tscn now embeds the HUD instance with ScoreLabel + update_score(7) works via the embedded instance. `godot --headless ... -a test/hud_test.gd` → 11/11 pass; full `-a test/` → 112/112 across 10 suites.
- Learning: `[node name="X" parent="." instance=ExtResource("...")]` in a .tscn loads the embedded scene's root WITHOUT explicit type= — it inherits the embedded scene's type. So replacing `type="CanvasLayer"` with `instance=ExtResource(hud.tscn)` preserves the CanvasLayer type while transparently adding the script + children. The parent .tscn's `@onready var hud: CanvasLayer = $HUD` keeps resolving because the instanced root IS still a CanvasLayer. This is the cleanest "promote inline node to its own scene" refactor — no script attached at parent level, no NodePath churn. Important caveat: if game.tscn had previously overridden any HUD properties (e.g. `transform`), those overrides would be lost; here HUD was bare so the migration is lossless.

